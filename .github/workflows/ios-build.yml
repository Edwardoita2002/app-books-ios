name: iOS Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      # Genera la carpeta /ios nativa en el servidor de GitHub para que no falle
      - name: Run Expo Prebuild (iOS)
        run: npx expo prebuild --platform ios

      # Instala las dependencias nativas de iOS (Cocoapods)
      - name: Install Pods
        run: |
          cd ios
          pod install

      # Compila la aplicación en Xcode sin necesidad de firma de Apple
      - name: Build iOS App
        run: |
          cd ios
          xcodebuild -workspace *.xcworkspace \
                     -scheme $(xcodebuild -list -json | node -e "const fs = require('fs'); const data = JSON.parse(fs.readFileSync(0, 'utf-8')); console.log(data.workspace.schemes[0]);") \
                     -configuration Release \
                     -sdk iphoneos \
                     clean build \
                     CODE_SIGNING_ALLOWED=NO

      # Empaqueta el archivo final en un instalador .IPA
      - name: Package into Unsigned IPA
        run: |
          mkdir -p build/Payload
          cp -r ios/build/Build/Products/Release-iphoneos/*.app build/Payload/
          cd build
          zip -r ../app-unsigned.ipa Payload

      # Sube el instalador a GitHub para que lo puedas descargar en Windows
      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-app-unsigned
          path: app-unsigned.ipa